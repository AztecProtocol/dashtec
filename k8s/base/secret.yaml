# This is a template - DO NOT commit actual secrets to git
# Create actual secrets using kubectl or your secret management tool

apiVersion: v1
kind: Secret
metadata:
  name: dashtec-secrets
type: Opaque
stringData:
  # Database connection string
  # Format: postgresql://user:password@host:port/database
  database-url: ""

  # Optional: Redis connection string for caching
  # Format: redis://[:password@]host:port/db
  redis-url: ""
